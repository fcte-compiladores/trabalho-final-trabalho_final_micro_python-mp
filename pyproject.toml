[project]
name = "mp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "lark-parser>=0.12.0",
]

[project.scripts]
mp = "mp.cli:main"


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"